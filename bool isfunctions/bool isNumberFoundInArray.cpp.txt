
bool isNumberFoundInArray(int Array[100] , int arrayLength)
{
    

    for(int i = 0 ; i < arrayLength ; i++)
    {
        if(number == Array[i])
            return true ; 
    }

    return false ;
}